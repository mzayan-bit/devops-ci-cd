name: Second workflow

on:
  workflow_dispatch

jobs:
  second-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Show text file
        run: cat secondfile.txt

      - name: Run python file
        run: python3 secondfile.py
