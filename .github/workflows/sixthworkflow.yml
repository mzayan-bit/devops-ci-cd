name: Sixth workflow (Pull Request)

on:
  pull_request:
    branches:
      - main

jobs:
  pr-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: PR message
        run: echo "Pull Request workflow is running!"

      - name: Show files
        run: ls
