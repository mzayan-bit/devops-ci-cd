name: Third workflow (Filtered Branch)

on:
  push:
    branches:
      - main

jobs:
  third-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Show message
        run: echo "This runs only on main branch"

      - name: Show files
        run: ls
